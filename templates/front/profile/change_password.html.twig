{% extends 'front/layout.html.twig' %}

{% block front_content %}
    <main class="password-page">
        <section class="password-card">
            <div class="password-bg" aria-hidden="true">
                <span class="password-orb password-orb-one"></span>
                <span class="password-orb password-orb-two"></span>
            </div>

            <header class="password-header">
                <div class="password-icon">
                    <i data-lucide="key-round"></i>
                </div>

                <span class="password-kicker">
                    <i data-lucide="shield-check"></i>
                    Security Settings
                </span>

                <h1>Change Password</h1>

                <p>
                    Update your password to keep your account secure.
                </p>
            </header>

            {% for message in app.flashes('error') %}
                <div class="password-alert password-alert-error">
                    <i data-lucide="triangle-alert"></i>
                    <span>{{ message }}</span>
                </div>
            {% endfor %}

            {% for message in app.flashes('success') %}
                <div class="password-alert password-alert-success">
                    <i data-lucide="circle-check"></i>
                    <span>{{ message }}</span>
                </div>
            {% endfor %}

            <form method="post" onsubmit="return confirmChangePassword()" class="password-form">
                <div class="password-form-group">
                    <label for="currentPassword">Current Password</label>

                    <div class="password-input-wrap">
                        <i data-lucide="lock"></i>

                        <input
                            id="currentPassword"
                            type="password"
                            name="currentPassword"
                            required
                            class="password-input"
                            placeholder="Enter your current password"
                        >
                    </div>
                </div>

                <div class="password-form-group">
                    <label for="newPassword">New Password</label>

                    <div class="password-input-wrap">
                        <i data-lucide="key-round"></i>

                        <input
                            id="newPassword"
                            type="password"
                            name="newPassword"
                            required
                            minlength="6"
                            class="password-input"
                            placeholder="Enter your new password"
                        >
                    </div>
                </div>

                <div class="password-form-group">
                    <label for="confirmPassword">Confirm New Password</label>

                    <div class="password-input-wrap">
                        <i data-lucide="badge-check"></i>

                        <input
                            id="confirmPassword"
                            type="password"
                            name="confirmPassword"
                            required
                            minlength="6"
                            class="password-input"
                            placeholder="Confirm your new password"
                        >
                    </div>
                </div>

                <button type="submit" class="password-submit-btn">
                    <i data-lucide="save"></i>
                    Update Password
                </button>
            </form>

            <div class="password-back-row">
                <a href="{{ path('front_profile') }}">
                    <i data-lucide="arrow-left"></i>
                    Back to Profile
                </a>
            </div>
        </section>
    </main>

    <style>
        .password-page,
        .password-page * {
            box-sizing: border-box;
        }

        .password-page {
            max-width: 720px;
            margin: 0 auto;
            color: #0f172a;
        }

        .password-card {
            position: relative;
            overflow: hidden;
            border-radius: 30px;
            padding: clamp(26px, 4vw, 40px);
            background: #ffffff;
            border: 1px solid #e2e8f0;
            box-shadow: 0 24px 70px rgba(15, 23, 42, 0.12);
        }

        .password-card::before {
            content: "";
            position: absolute;
            inset: 0;
            height: 220px;
            background:
                radial-gradient(circle at 78% 30%, rgba(37, 99, 235, 0.24), transparent 34%),
                linear-gradient(135deg, #071126 0%, #111827 52%, #172554 100%);
            pointer-events: none;
        }

        .password-card::after {
            content: "";
            position: absolute;
            top: -45%;
            left: -45%;
            width: 190%;
            height: 430px;
            background:
                conic-gradient(
                    from 0deg,
                    transparent,
                    rgba(59, 130, 246, 0.22),
                    transparent,
                    rgba(56, 189, 248, 0.16),
                    transparent,
                    rgba(96, 165, 250, 0.18),
                    transparent
                );
            filter: blur(80px);
            opacity: 0.72;
            animation: passwordAurora 18s linear infinite;
            pointer-events: none;
        }

        .password-bg {
            position: absolute;
            inset: 0;
            pointer-events: none;
            overflow: hidden;
        }

        .password-orb {
            position: absolute;
            border-radius: 999px;
            filter: blur(12px);
            opacity: 0.38;
            mix-blend-mode: screen;
        }

        .password-orb-one {
            width: 190px;
            height: 190px;
            right: 9%;
            top: 4%;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.95), transparent 66%);
            animation: passwordFloatOne 8s ease-in-out infinite;
        }

        .password-orb-two {
            width: 130px;
            height: 130px;
            left: 9%;
            top: 70px;
            background: radial-gradient(circle, rgba(14, 165, 233, 0.8), transparent 68%);
            animation: passwordFloatTwo 10s ease-in-out infinite;
        }

        .password-header,
        .password-alert,
        .password-form,
        .password-back-row {
            position: relative;
            z-index: 1;
        }

        .password-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .password-icon {
            width: 92px;
            height: 92px;
            display: grid;
            place-items: center;
            margin: 0 auto 18px;
            border-radius: 28px;
            color: #ffffff;
            background:
                radial-gradient(circle at 35% 25%, rgba(125, 211, 252, 0.95), transparent 34%),
                linear-gradient(135deg, #2563eb, #1d4ed8);
            box-shadow:
                0 0 0 10px rgba(37, 99, 235, 0.18),
                0 22px 46px rgba(37, 99, 235, 0.38);
        }

        .password-icon svg {
            width: 40px;
            height: 40px;
        }

        .password-kicker {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            width: max-content;
            padding: 7px 12px;
            margin-bottom: 14px;
            border-radius: 999px;
            color: #bfdbfe;
            background: rgba(37, 99, 235, 0.18);
            border: 1px solid rgba(147, 197, 253, 0.24);
            font-size: 11px;
            font-weight: 950;
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        .password-kicker svg {
            width: 15px;
            height: 15px;
        }

        .password-header h1 {
            margin: 0;
            color: #ffffff;
            font-size: clamp(36px, 5vw, 56px);
            line-height: 0.98;
            letter-spacing: -0.06em;
            font-weight: 950;
        }

        .password-header p {
            margin: 12px 0 0;
            color: #cbd5e1;
            font-size: 17px;
            line-height: 1.5;
            font-weight: 650;
        }

        .password-alert {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 14px 16px;
            margin-bottom: 16px;
            border-radius: 16px;
            font-weight: 800;
            line-height: 1.4;
        }

        .password-alert svg {
            width: 20px;
            height: 20px;
            flex: 0 0 auto;
        }

        .password-alert-error {
            color: #991b1b;
            background: #fee2e2;
            border: 1px solid #fecaca;
        }

        .password-alert-success {
            color: #166534;
            background: #dcfce7;
            border: 1px solid #86efac;
        }

        .password-form {
            display: grid;
            gap: 18px;
            margin-top: 28px;
        }

        .password-form-group {
            display: grid;
            gap: 8px;
        }

        .password-form-group label {
            color: #334155;
            font-size: 14px;
            font-weight: 900;
        }

        .password-input-wrap {
            min-height: 60px;
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 0 16px;
            border-radius: 18px;
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            transition: 180ms ease;
        }

        .password-input-wrap:focus-within {
            background: #ffffff;
            border-color: #93c5fd;
            box-shadow:
                0 0 0 4px rgba(37, 99, 235, 0.10),
                0 14px 30px rgba(15, 23, 42, 0.07);
        }

        .password-input-wrap svg {
            width: 22px;
            height: 22px;
            color: #2563eb;
            flex: 0 0 auto;
        }

        .password-input {
            width: 100%;
            min-height: 56px;
            border: 0;
            outline: 0;
            background: transparent;
            color: #0f172a;
            font-size: 16px;
            font-weight: 750;
        }

        .password-input::placeholder {
            color: #94a3b8;
        }

        .password-submit-btn {
            min-height: 58px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            width: 100%;
            margin-top: 6px;
            border: 0;
            border-radius: 18px;
            color: #ffffff;
            background: linear-gradient(135deg, #2563eb, #1d4ed8);
            box-shadow: 0 18px 38px rgba(37, 99, 235, 0.30);
            font-size: 16px;
            font-weight: 950;
            cursor: pointer;
            transition: 180ms ease;
        }

        .password-submit-btn svg {
            width: 20px;
            height: 20px;
        }

        .password-submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 22px 44px rgba(37, 99, 235, 0.38);
        }

        .password-back-row {
            display: flex;
            justify-content: center;
            margin-top: 22px;
        }

        .password-back-row a {
            min-height: 46px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 0 14px;
            border-radius: 999px;
            color: #1d4ed8;
            background: #eff6ff;
            border: 1px solid #dbeafe;
            text-decoration: none;
            font-size: 14px;
            font-weight: 900;
            transition: 180ms ease;
        }

        .password-back-row a:hover {
            transform: translateY(-2px);
            background: #dbeafe;
        }

        .password-back-row svg {
            width: 17px;
            height: 17px;
        }

        @keyframes passwordAurora {
            to {
                transform: rotate(360deg);
            }
        }

        @keyframes passwordFloatOne {
            0%, 100% {
                transform: translate3d(0, 0, 0) scale(1);
            }

            50% {
                transform: translate3d(-34px, 26px, 0) scale(1.12);
            }
        }

        @keyframes passwordFloatTwo {
            0%, 100% {
                transform: translate3d(0, 0, 0) scale(1);
            }

            50% {
                transform: translate3d(38px, -18px, 0) scale(1.14);
            }
        }

        @media (prefers-reduced-motion: reduce) {
            *,
            *::before,
            *::after {
                animation-duration: 0.001ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.001ms !important;
                scroll-behavior: auto !important;
            }
        }

        @media (max-width: 640px) {
            .password-card {
                border-radius: 24px;
                padding: 24px 18px;
            }

            .password-icon {
                width: 82px;
                height: 82px;
                border-radius: 24px;
            }

            .password-icon svg {
                width: 36px;
                height: 36px;
            }

            .password-header h1 {
                font-size: 38px;
            }
        }
    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script>
        function confirmChangePassword() {
            const ok = confirm("Are you sure you want to change your password?");

            if (ok) {
                if (typeof announceText === 'function') {
                    announceText("Changing your password");
                }

                if (typeof speakText === 'function') {
                    speakText("Changing your password");
                }
            }

            return ok;
        }

        document.addEventListener('DOMContentLoaded', function () {
            if (window.lucide) {
                lucide.createIcons();
            }
        });
    </script>
{% endblock %}